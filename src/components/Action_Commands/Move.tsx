import React from 'react'

/* 
if (action === "Move" || action === "M") {
    //Move from one location to an 'adjacent' one
    await locationMove();
 */

const Move = () => {
  return (
    <div>Move</div>
  )
}

export default Move