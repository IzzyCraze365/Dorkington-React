import React from 'react'

/* if (action === "Help" || action === "H") {
    //Brings up the Help Menu
    helpMenu(); */

const Help = () => {
  return (
    <div>Help</div>
  )
}

export default Help