import React from 'react'

/* 
if (action === "Backpack" || action === "B") {
    // Displays inventory
    itemDisplay(hero.inventory);
 */

const Backpack = () => {
  return (
    <div>Backpack</div>
  )
}

export default Backpack